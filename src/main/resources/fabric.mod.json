{
  "schemaVersion": 1,
  "id": "${id}",
  "version": "${version}",

  "name": "FabLabs ${libname} version ${libver}",
  "icon": "assets/fablabs-client-command-api-v1/icon.png",
  "description": "API for client-sided chat commands.",
  "license": "MIT",
  "contact": {
    "sources": "https://github.com/${repo}"
  },
  "environment": "client",
  "entrypoints": {
    "client": [
    ]
  },
  "mixins": [
    "mixins.fablabs-client-command-api-v1.json"
  ],
  "depends": {
    "fabricloader": ">=0.4.0"
  },
  "custom": {
    "modmenu:api": true
  }
}
